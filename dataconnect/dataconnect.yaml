specVersion: "v1"
serviceId: "triplematch"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "triplematch-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
